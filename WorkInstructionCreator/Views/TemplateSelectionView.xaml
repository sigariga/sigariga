<UserControl x:Class="WorkInstructionCreator.Views.TemplateSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="300">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Search -->
        <TextBox Grid.Row="0" 
                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                 Style="{StaticResource MaterialDesignFilledTextBox}"
                 materialDesign:HintAssist.Hint="Search templates..."
                 Margin="8">
            <TextBox.InputBindings>
                <KeyBinding Key="Enter" Command="{Binding ApplyFiltersCommand}"/>
            </TextBox.InputBindings>
        </TextBox>

        <!-- Category Filter -->
        <ComboBox Grid.Row="1" 
                  ItemsSource="{Binding Categories}"
                  SelectedItem="{Binding SelectedCategory}"
                  Style="{StaticResource MaterialDesignFilledComboBox}"
                  materialDesign:HintAssist.Hint="Category"
                  Margin="8,0,8,8"/>

        <!-- Templates List -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding FilteredTemplates}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <materialDesign:Card Margin="8,4" 
                                           Cursor="Hand"
                                           materialDesign:ShadowAssist.ShadowDepth="Depth1">
                            <materialDesign:Card.InputBindings>
                                <MouseBinding MouseAction="LeftClick" 
                                            Command="{Binding DataContext.SelectTemplateCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"/>
                            </materialDesign:Card.InputBindings>
                            
                            <Border Padding="12">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Template Name -->
                                    <TextBlock Grid.Row="0" 
                                             Text="{Binding Name}"
                                             FontWeight="Bold"
                                             FontSize="14"
                                             TextWrapping="Wrap"
                                             Margin="0,0,0,4"/>

                                    <!-- Category -->
                                    <Border Grid.Row="1" 
                                          Background="{DynamicResource MaterialDesignChipBackground}"
                                          CornerRadius="12" 
                                          HorizontalAlignment="Left"
                                          Padding="8,2"
                                          Margin="0,0,0,4">
                                        <TextBlock Text="{Binding Category}" 
                                                 FontSize="10" 
                                                 Foreground="{DynamicResource MaterialDesignBody}"/>
                                    </Border>

                                    <!-- Description -->
                                    <TextBlock Grid.Row="2" 
                                             Text="{Binding Description}"
                                             TextWrapping="Wrap"
                                             FontSize="12"
                                             Foreground="{DynamicResource MaterialDesignBodyLight}"
                                             Margin="0,0,0,8"/>

                                    <!-- Actions -->
                                    <StackPanel Grid.Row="3" 
                                              Orientation="Horizontal" 
                                              HorizontalAlignment="Right">
                                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                              ToolTip="Delete Template"
                                              Visibility="{Binding IsBuiltIn, Converter={StaticResource InverseBooleanToVisibilityConverter}}"
                                              Command="{Binding DataContext.DeleteTemplateCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                              CommandParameter="{Binding}">
                                            <materialDesign:PackIcon Kind="Delete" Foreground="{DynamicResource MaterialDesignValidationError}"/>
                                        </Button>
                                        
                                        <materialDesign:PackIcon Kind="Lock" 
                                                               VerticalAlignment="Center"
                                                               Margin="4"
                                                               Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                               Visibility="{Binding IsBuiltIn, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                               ToolTip="Built-in template"/>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <!-- Hover Effect -->
                            <materialDesign:Card.Style>
                                <Style TargetType="materialDesign:Card" BasedOn="{StaticResource {x:Type materialDesign:Card}}">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="materialDesign:ShadowAssist.ShadowDepth" Value="Depth3"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </materialDesign:Card.Style>
                        </materialDesign:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Loading Indicator -->
        <ProgressBar Grid.Row="2" 
                     IsIndeterminate="True"
                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                     Margin="8"/>

        <!-- Empty State -->
        <StackPanel Grid.Row="2" 
                    VerticalAlignment="Center" 
                    HorizontalAlignment="Center"
                    Visibility="{Binding FilteredTemplates.Count, Converter={StaticResource ZeroToVisibilityConverter}}">
            <materialDesign:PackIcon Kind="FileDocumentOutline" 
                                   Width="48" Height="48" 
                                   Foreground="{DynamicResource MaterialDesignBodyLight}"
                                   HorizontalAlignment="Center"/>
            <TextBlock Text="No templates found" 
                     FontSize="16" 
                     Foreground="{DynamicResource MaterialDesignBodyLight}"
                     HorizontalAlignment="Center"
                     Margin="0,8,0,0"/>
            <TextBlock Text="Try adjusting your search or category filter" 
                     FontSize="12" 
                     Foreground="{DynamicResource MaterialDesignBodyLight}"
                     HorizontalAlignment="Center"
                     TextWrapping="Wrap"
                     TextAlignment="Center"
                     Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Add Template Button -->
        <Button Grid.Row="2" 
                Style="{StaticResource MaterialDesignFloatingActionMiniButton}"
                Command="{Binding CreateNewTemplateCommand}"
                ToolTip="Create New Template"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Margin="16">
            <materialDesign:PackIcon Kind="Plus"/>
        </Button>
    </Grid>
</UserControl>